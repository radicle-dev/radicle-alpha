(load-ns! "prelude/test-macro.rad")
(load-ns! "prelude/basic.rad")
(load-ns! "prelude/patterns.rad")
(load-ns! "prelude/macros.rad")

(ns foo)

(require prelude/patterns '[match-pat])
(require prelude/macros '[defn])

(defn foo [x]
  (match x
         {:hello 'y} y
         [:no 'n] n))

(ns bar)

(require foo '[foo] blob)

(put-str! (show (blob/foo {:hello 42})))
