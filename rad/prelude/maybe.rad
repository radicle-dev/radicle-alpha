(ns prelude.maybe "Functions for dealing with optional values.")

(require prelude.test-macro ['test])
(require prelude.patterns [])

(def cat-maybes
  (fn [xs]
    (foldr (fn [x_ acc]
             (match x_
                    (/just 'x) (cons x acc)
                    _          acc))
           []
           xs)))
